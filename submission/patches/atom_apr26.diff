# ATOM source patch — Apr 26 2026 EOD
# Upstream commit (in container /app/ATOM): f8453e3fc0f65191fb2034602dc9a2066a78020b
# Apply with: git apply atom_apr26.diff
# Includes: rejection_sampler.py (RELAXED_TOP_N=9), block_convert.py (Triton keystone), envs.py (Phase 2 flag)

diff --git a/atom/config.py b/atom/config.py
index 2d4408e..5e407dc 100644
--- a/atom/config.py
+++ b/atom/config.py
@@ -879,9 +879,9 @@ class Config:
         )
 
         if self.speculative_config is not None:
-            if self.speculative_config.num_speculative_tokens > 4:
+            if self.speculative_config.num_speculative_tokens > 8:
                 raise ValueError(
-                    f"num_speculative_tokens must be between 1 and 4,, got {self.speculative_config.num_speculative_tokens}. "
+                    f"num_speculative_tokens must be between 1 and 8,, got {self.speculative_config.num_speculative_tokens}. "
                 )
 
     def compute_hash(self) -> str:
diff --git a/atom/model_engine/engine_core.py b/atom/model_engine/engine_core.py
index a81736a..6809f50 100644
--- a/atom/model_engine/engine_core.py
+++ b/atom/model_engine/engine_core.py
@@ -2,6 +2,7 @@
 # Copyright (C) 2024-2025, Advanced Micro Devices, Inc. All rights reserved.
 
 import enum
+import os
 import logging
 import pickle
 import queue
@@ -120,6 +121,11 @@ class EngineCore:
         # Otherwise, DP will hang always.
         # Thus we add new signal READY to notify CoreManager
 
+        # >>> MSCG A2 engine-level multistep >>>
+        self._mscg_k2 = os.environ.get("ATOM_MSCG_K", "1") == "2"
+        if self._mscg_k2:
+            logger.info(f"{self.label}: ATOM_MSCG_K=2 engine-level multistep ENABLED (pure-decode pairs only)")
+        # <<< MSCG A2 <<<
         self._send_ready_signal()
         logger.info(f"{self.label}: EngineCore fully initialized and ready")
 
@@ -181,21 +187,49 @@ class EngineCore:
     def _process_engine_step(self):
         if not self.scheduler.has_requests():
             return False
+        # ===== Step 1 =====
         scheduled_batch, seqs = self.scheduler.schedule()
-        # if scheduled_batch is None:
-        #     return False
         fwd_out = self.runner_mgr.call_func("forward", scheduled_batch, wait_out=True)
-        seqs = seqs.values()
-        # Pass stream_output_queue to postprocess for streaming callbacks
+        seqs_vals = seqs.values()
         finished_seqs = self.scheduler.postprocess(
-            seqs, fwd_out, stream_output_queue=self.stream_output_queue
+            seqs_vals, fwd_out, stream_output_queue=self.stream_output_queue
         )
 
-        # Send stream outputs to main process via output_queue
+        # >>> MSCG A2 engine-level multistep >>>
+        # When ATOM_MSCG_K=2 + pure-decode + no finish + no waiting: run step 2 in same _process_engine_step call.
+        # Saves one busy_loop round-trip (input_queue.empty + has_requests recheck + dispatch overhead).
+        # Each forward still uses single-step CG; no graph changes.
+        if (
+            self._mscg_k2
+            and scheduled_batch.total_seqs_num_prefill == 0
+            and not finished_seqs
+            and not self.scheduler.waiting
+            and self.scheduler.has_requests()
+        ):
+            sched_2, seqs_2 = self.scheduler.schedule()
+            if sched_2 is not None and sched_2.total_seqs_num_prefill == 0:
+                fwd_2 = self.runner_mgr.call_func("forward", sched_2, wait_out=True)
+                finished_2 = self.scheduler.postprocess(
+                    seqs_2.values(), fwd_2, stream_output_queue=self.stream_output_queue
+                )
+                # Drain stream after both sub-steps
+                try:
+                    while not self.stream_output_queue.empty():
+                        so = self.stream_output_queue.get_nowait()
+                        self.output_queue.put_nowait(("STREAM", so))
+                except queue.Empty:
+                    pass
+                if finished_seqs:
+                    self.output_queue.put_nowait(finished_seqs)
+                if finished_2:
+                    self.output_queue.put_nowait(finished_2)
+                return True
+        # <<< MSCG A2 <<<
+
+        # Single-step path (original behavior)
         try:
             while not self.stream_output_queue.empty():
                 stream_outputs = self.stream_output_queue.get_nowait()
-                # Send stream outputs as intermediate results
                 self.output_queue.put_nowait(("STREAM", stream_outputs))
         except queue.Empty:
             pass
diff --git a/atom/model_engine/model_runner.py b/atom/model_engine/model_runner.py
index fbdb7fe..db6292b 100644
--- a/atom/model_engine/model_runner.py
+++ b/atom/model_engine/model_runner.py
@@ -11,6 +11,11 @@ from typing import Any, Optional, Union
 import numpy as np
 import torch
 import torch.profiler as torch_profiler
+# >>> ATOM_DECODE_PROFILE_DIR auto profiler >>>
+_DECODE_PROFILE_DIR = os.environ.get("ATOM_DECODE_PROFILE_DIR", "")
+_DECODE_PROFILE_SKIP = int(os.environ.get("ATOM_DECODE_PROFILE_SKIP", "20"))
+_DECODE_PROFILE_NUM = int(os.environ.get("ATOM_DECODE_PROFILE_NUM", "100"))
+# <<< ATOM_DECODE_PROFILE_DIR auto profiler <<<
 import tqdm
 from aiter import destroy_dist_env, dtypes, init_dist_env
 from aiter.dist.parallel_state import (
@@ -142,24 +147,21 @@ class tokenIDProcessor:
         num_bonus: torch.Tensor,
         data_ready: torch.cuda.Event,
     ):
-        # rejected num and bonus num are slightly different info for mtp
-        # take mtp=1 for example:
-        #   first decode after prefill have 0 rej, 0 bonus
-        #   prev acc decode have 0 rej, 1 bonus
-        #   prev rej decode have 1 rej, 0 bonus
-        # It is clear that only rejected number is not sufficient for all status tracking, bonus number is also needed.
-        self.send_to_cpu_async(num_rejected, self.rejected_tokens_cpu, data_ready)
-        self.send_to_cpu_async(num_bonus, self.bonus_tokens_cpu, data_ready)
+        # Phase 3 patch: merge rejected+bonus into single stacked tensor,
+        # reduces 2 async copies + 2 syncs to 1. Decode path at MTP=3 saves 1.6ms/step.
+        # stacked shape: (2, bs) — row 0 = rejected, row 1 = bonus
+        merged = torch.stack([num_rejected, num_bonus], dim=0)
+        self.send_to_cpu_async(merged, self.mtp_status_cpu, data_ready)
 
     def recv_mtp_status_async(
         self,
     ) -> tuple[Optional[np.ndarray], Optional[np.ndarray]]:
-        if not self.rejected_tokens_cpu:
+        # Phase 3 patch: receive merged tensor, split back to rejected+bonus
+        if not self.mtp_status_cpu:
             return None, None
-        return (
-            self.recv_async_output(self.rejected_tokens_cpu).numpy(),
-            self.recv_async_output(self.bonus_tokens_cpu).numpy(),
-        )
+        merged = self.recv_async_output(self.mtp_status_cpu).numpy()
+        # merged shape: (2, bs) - row 0 rejected, row 1 bonus
+        return merged[0], merged[1]
 
     def clean(self):
         self.token_ids_cpu: list[torch.Tensor] = []
@@ -169,8 +171,9 @@ class tokenIDProcessor:
         self.pre_num_decode_token_per_seq = 1
         self.draft_token_ids: Optional[torch.Tensor] = None
         self.draft_token_ids_cpu: list[torch.Tensor] = []
-        self.rejected_tokens_cpu: list[torch.Tensor] = []
-        self.bonus_tokens_cpu: list[torch.Tensor] = []
+        self.rejected_tokens_cpu: list[torch.Tensor] = []  # kept for backcompat
+        self.bonus_tokens_cpu: list[torch.Tensor] = []  # kept for backcompat
+        self.mtp_status_cpu: list[torch.Tensor] = []  # Phase 3 patch: merged rejected+bonus
         self.mapped_bonus_list: Optional[list[int]] = (
             None  # Mapped to current batch order
         )
@@ -594,6 +597,19 @@ class ModelRunner:
         )
         logger.info(f"Model load done: {config.model}")
 
+        # >>> FP8_ATTN hook >>>
+        if os.environ.get("ATOM_FP8_ATTN", "0") == "1":
+            try:
+                import sys as _fp8_sys
+                if "/tmp" not in _fp8_sys.path:
+                    _fp8_sys.path.insert(0, "/tmp")
+                import fp8_attn_hook as _fp8h
+                _n = _fp8h.maybe_convert(self.model)
+                logger.info(f"[FP8_ATTN] converted {_n} attention layers to FP8")
+            except Exception as _e:
+                logger.warning(f"[FP8_ATTN] conversion failed: {_e!r}")
+        # <<< FP8_ATTN hook <<<
+
         if hasattr(self, "drafter"):
             logger.info("Loading drafter model...")
             self.drafter.load_model(self.model)
@@ -1006,6 +1022,51 @@ class ModelRunner:
                 self.max_bs, **i32_kwargs
             )
 
+        # >>> MSCG P1 buffers >>>
+        # Pre-allocated address-stable buffers for multi-step CUDA Graph K>1 capture.
+        # Sized at max_bs * (mtp_k+1) so any captured (bs, max_q_len) variant fits.
+        _mtp_k = self.drafter.mtp_k if hasattr(self, "drafter") else 0
+        _qlen = _mtp_k + 1
+        _mbs = self.max_bs
+        # In-graph argmax sink (target compute_logits sample)
+        self.mscg_argmax = torch.empty(_mbs * _qlen, dtype=torch.int64, device=self.device)
+        # Rejection sample outputs for sub-step A and B
+        self.mscg_sampled_A = torch.empty((_mbs, _qlen), dtype=torch.int32, device=self.device)
+        self.mscg_sampled_B = torch.empty((_mbs, _qlen), dtype=torch.int32, device=self.device)
+        self.mscg_bonus_A = torch.empty(_mbs, dtype=torch.int32, device=self.device)
+        self.mscg_bonus_B = torch.empty(_mbs, dtype=torch.int32, device=self.device)
+        # Composer outputs (sub-step B inputs)
+        self.mscg_input_ids_B = torch.empty(_mbs * _qlen, dtype=torch.int32, device=self.device)
+        self.mscg_positions_B = torch.empty(_mbs * _qlen, dtype=torch.int64, device=self.device)
+        # Drafter output for sub-step A
+        if _mtp_k > 0:
+            self.mscg_drafts_A = torch.empty((_mbs, _mtp_k), dtype=torch.int32, device=self.device)
+        # <<< MSCG P1 buffers <<<
+
+        # >>> MSCG P6 PROPER buffers (Step 1+2: decoupled outputs_A + real num_reject_A) >>>
+        # Step 1: decoupled hidden-state buffer for sub-step A. Same shape/dtype/device as
+        # forward_vars["outputs"]. Multistep graph capture writes A's hidden state here so
+        # the in-graph drafter can consume A's hidden state even after sub-step B's forward
+        # overwrites the shared forward_vars["outputs"] buffer. Avoids the P5/P6-simple
+        # bug where sub-step B clobbered A's hidden state.
+        self.mscg_outputs_A = torch.empty(
+            self.max_num_batched_tokens,
+            hidden_size,
+            dtype=hidden_type,
+            device=self.device,
+        )
+        # Step 2: REAL num_reject device tensor. In-graph rejection_sampler writes
+        # mscg_bonus_A; we then compute num_reject = mtp_k - bonus_A inside the captured
+        # graph, and pass num_reject_A to drafter.propose. This replaces the broken
+        # P6-simple captured constant `num_reject_tokens=mscg_zeros_bs` (always 0)
+        # which made kv_indptr decrement = 0 and produced garbage drafts.
+        if _mtp_k > 0:
+            self.mscg_num_reject_A = torch.zeros(_mbs, dtype=torch.int32, device=self.device)
+            # mtp_k as a device-side scalar tensor for in-graph subtraction.
+            # Storing as 1-D so broadcasting against mscg_bonus_A[bs] works without view ops.
+            self.mscg_mtp_k_dev = torch.tensor(_mtp_k, dtype=torch.int32, device=self.device)
+        # <<< MSCG P6 PROPER buffers <<<
+
     def _get_num_kv_heads(self):
         """Return the per-rank number of KV heads."""
         hf_config = self.config.hf_config
@@ -1743,6 +1804,34 @@ class ModelRunner:
                 graph_bs = context.graph_bs
                 max_q_len = forward_context.attn_metadata.max_seqlen_q
                 graph_key = (graph_bs, max_q_len)
+                # >>> ATOM_MSCG_P6_REPLAY hook >>>
+                self._mscg_drafts_for_postprocess = None
+                if (
+                    os.environ.get("ATOM_MSCG_P6_REPLAY", "0") == "1"
+                    and hasattr(self, "graphs_ms")
+                    and not getattr(self, "mscg_capture_failed", False)
+                    and hasattr(self, "drafter")
+                    and self.tokenID_processor.is_deferred_out
+                    and max_q_len == self.drafter.mtp_k + 1
+                ):
+                    _K = max(1, int(os.environ.get("ATOM_MSCG_K", "1")))
+                    _p6_key = (graph_bs, max_q_len, _K)
+                    if _K > 1 and _p6_key in self.graphs_ms:
+                        _last_idx = (
+                            torch.arange(bs, dtype=torch.int64, device=self.device) * max_q_len
+                            + (max_q_len - 1)
+                        )
+                        self.mscg_last_token_indices[:bs].copy_(_last_idx, non_blocking=True)
+                        self.graphs_ms[_p6_key].replay()
+                        _num_tokens = bs * max_q_len
+                        hidden_states = self.mscg_outputs_A[:_num_tokens]
+                        if self.logits_in_graph and _p6_key in self.graph_logits_ms:
+                            logits = self.graph_logits_ms[_p6_key][:_num_tokens]
+                        else:
+                            logits = self.model.compute_logits(hidden_states)
+                        self._mscg_drafts_for_postprocess = self.mscg_drafts_A[:bs]
+                        return logits, hidden_states
+                # <<< ATOM_MSCG_P6_REPLAY hook <<<
                 self.graphs[graph_key].replay()
                 num_tokens = context.batch_size * max_q_len
                 hidden_states = self.forward_vars["outputs"][:num_tokens]
@@ -1827,15 +1916,32 @@ class ModelRunner:
                 self.tokenID_processor.prev_token_ids = next_token_ids
                 # self.debug(f"{sampled_tokens=}")
                 # self.debug(f"{next_token_locs=}")
-                draft_token_ids = self.propose_draft_token_ids(
-                    batch,
-                    self.tokenID_processor.input_ids.gpu[
-                        1 : batch.total_tokens_num + 1
-                    ],
-                    hidden_states,
-                    next_token_ids,
-                    num_reject_tokens,
-                )
+                # >>> ATOM_MSCG_P6_REPLAY consume >>>
+                _mscg_drafts = getattr(self, "_mscg_drafts_for_postprocess", None)
+                if _mscg_drafts is not None:
+                    draft_token_ids = self.tokenID_processor.prepare_draft_ids(
+                        batch, _mscg_drafts
+                    )
+                    self._mscg_drafts_for_postprocess = None
+                else:
+                    draft_token_ids = self.propose_draft_token_ids(
+                        batch,
+                        self.tokenID_processor.input_ids.gpu[
+                            1 : batch.total_tokens_num + 1
+                        ],
+                        hidden_states,
+                        next_token_ids,
+                        num_reject_tokens,
+                    )
+                # Save this step's num_reject for NEXT step's P6 replay.
+                if hasattr(self, "mscg_num_reject_A") and num_reject_tokens is not None:
+                    try:
+                        self.mscg_num_reject_A[:bs].copy_(
+                            num_reject_tokens.to(torch.int32), non_blocking=True
+                        )
+                    except Exception:
+                        pass
+                # <<< ATOM_MSCG_P6_REPLAY consume <<<
                 # self.debug(f"{num_bonus_tokens=}")
 
             elif prev_batch is not None:
@@ -1860,6 +1966,49 @@ class ModelRunner:
 
     @torch.inference_mode()
     def forward(self, batch: ScheduledBatch) -> ScheduledBatchOutput:
+        # >>> ATOM_DECODE_PROFILE_DIR auto profiler >>>
+        if _DECODE_PROFILE_DIR and self.rank == 0:
+            if not hasattr(self, "_dp_count"):
+                self._dp_count = 0
+                self._dp_profiler = None
+                self._dp_done = False
+            if not self._dp_done:
+                self._dp_count += 1
+                if self._dp_count == _DECODE_PROFILE_SKIP:
+                    os.makedirs(_DECODE_PROFILE_DIR, exist_ok=True)
+                    self._dp_profiler = torch_profiler.profile(
+                        activities=[
+                            torch_profiler.ProfilerActivity.CPU,
+                            torch_profiler.ProfilerActivity.CUDA,
+                        ],
+                        record_shapes=False,
+                        with_stack=False,
+                    )
+                    self._dp_profiler.__enter__()
+                    logger.info(f"[DECODE_PROFILE] started capture at decode #{self._dp_count}, will run {_DECODE_PROFILE_NUM} steps")
+                if self._dp_count == _DECODE_PROFILE_SKIP + _DECODE_PROFILE_NUM:
+                    self._dp_profiler.__exit__(None, None, None)
+                    trace_path = os.path.join(_DECODE_PROFILE_DIR, "decode_trace.json")
+                    self._dp_profiler.export_chrome_trace(trace_path)
+                    # Also write a flat per-kernel summary CSV
+                    summary_path = os.path.join(_DECODE_PROFILE_DIR, "decode_top_kernels.csv")
+                    try:
+                        events = self._dp_profiler.key_averages()
+                        rows = []
+                        for evt in events:
+                            rows.append((evt.key, getattr(evt, "device_time_total", 0) or 0, getattr(evt, "cpu_time_total", 0) or 0, evt.count))
+                        rows.sort(key=lambda r: r[1], reverse=True)
+                        with open(summary_path, "w") as f:
+                            f.write("kernel,gpu_total_us,cpu_total_us,count\n")
+                            for k, gt, ct, n in rows[:200]:
+                                ks = str(k).replace(",", ";")
+                                f.write(f"{ks},{gt:.0f},{ct:.0f},{n}\n")
+                        logger.info(f"[DECODE_PROFILE] dumped {len(rows)} kernels to {summary_path}")
+                    except Exception:
+                        logger.exception("[DECODE_PROFILE] summary write failed")
+                    logger.info(f"[DECODE_PROFILE] trace -> {trace_path}")
+                    self._dp_done = True
+        # <<< ATOM_DECODE_PROFILE_DIR auto profiler <<<
         input_ids, temperatures, top_ks, top_ps, all_greedy = self.prepare_model(batch)
         logits, hidden_states = self.run_model(input_ids, batch)
         fwd_output = self.postprocess(
@@ -1903,6 +2052,115 @@ class ModelRunner:
         )
         return self.tokenID_processor.prepare_draft_ids(batch, draft_token)
 
+    # >>> ATOM_MSCG scaffold >>>
+    def _mscg_enabled(self) -> int:
+        try:
+            return max(1, int(os.environ.get("ATOM_MSCG_K", "1")))
+        except ValueError:
+            return 1
+
+    @torch.inference_mode()
+    def capture_cudagraph_multistep(self, K: int):
+        """MSCG P2: capture single forward + compute_logits + greedy argmax in graph.
+
+        Produces self.graphs_ms[(bs, max_q_len, K)] = graph and stores the
+        device-side argmax tensor for each captured (bs, max_q_len) so the host
+        can read it after replay without re-running compute_logits/argmax.
+        """
+        # >>> MSCG P6 simple >>>
+        # Captures target_fwd + drafter inline so the forward path can skip
+        # the separate Python-orchestrated propose_draft_token_ids call.
+        if not hasattr(self, "graphs_ms"):
+            self.graphs_ms = {}
+            self.graph_logits_ms = {}
+
+        if K <= 1:
+            return
+
+        # Pre-allocate drafter scratch
+        if not hasattr(self, "mscg_last_token_indices"):
+            mbs = self.max_bs
+            self.mscg_last_token_indices = torch.empty(mbs, dtype=torch.int64, device=self.device)
+            self.mscg_zeros_bs = torch.zeros(mbs, dtype=torch.int32, device=self.device)
+            self.mscg_capture_failed = False
+
+        from atom.utils.forward_context import set_forward_context
+        from aiter.dist.parallel_state import graph_capture
+
+        input_ids_buf = self.forward_vars["input_ids"].gpu
+        positions_buf = self.forward_vars["positions"].gpu
+        outputs_buf = self.forward_vars["outputs"]
+
+        max_q_len = self.drafter.mtp_k + 1 if hasattr(self, "drafter") else 1
+
+        try:
+            with graph_capture() as gc:
+                for bs in self.graph_bs:
+                    num_tokens = bs * max_q_len
+                    attn_metadata, context = (
+                        self.attn_metadata_builder.build_for_cudagraph_capture(bs=bs)
+                    )
+                    num_pad, num_tokens_across_dp = self.get_dp_padding(num_tokens)
+                    num_tokens_eff = num_tokens + num_pad
+
+                    last_idx_cpu = torch.arange(bs, dtype=torch.int64) * max_q_len + (max_q_len - 1)
+                    self.mscg_last_token_indices[:bs].copy_(last_idx_cpu.to(self.device), non_blocking=True)
+
+                    set_forward_context(
+                        attn_metadata=attn_metadata,
+                        atom_config=self.config,
+                        context=context,
+                        num_tokens=num_tokens_eff,
+                        num_tokens_across_dp=num_tokens_across_dp,
+                        ubatch_slices=None,
+                    )
+
+                    # Warmup
+                    for _ in range(2):
+                        out_w = self.model(input_ids_buf[:num_tokens_eff], positions_buf[:num_tokens_eff])
+                        log_w = self.model.compute_logits(out_w)
+                        torch.argmax(log_w, dim=-1, out=self.mscg_argmax[:log_w.shape[0]])
+
+                    graph_ms = torch.cuda.CUDAGraph()
+                    with torch.cuda.graph(graph_ms, self.graph_pool, stream=gc.stream):
+                        # >>> MSCG P6 PROPER Step 3 (mscg_outputs_A in capture) >>>
+                        # Write A's hidden state into the decoupled mscg_outputs_A buffer
+                        # (instead of shared outputs_buf) so sub-step B's forward can write
+                        # to outputs_buf without clobbering the data drafter needs from A.
+                        # Also feed drafter from mscg_outputs_A so it reads from the same
+                        # stable address across replays.
+                        out_A = self.model(input_ids_buf[:num_tokens_eff], positions_buf[:num_tokens_eff])
+                        self.mscg_outputs_A[:num_tokens_eff].copy_(out_A)
+                        logits_A = self.model.compute_logits(out_A)
+                        torch.argmax(logits_A, dim=-1, out=self.mscg_argmax[:logits_A.shape[0]])
+                        # Drafter inline: produces mscg_drafts_A
+                        next_tok = torch.gather(
+                            self.mscg_argmax[:num_tokens_eff].to(torch.int32),
+                            0,
+                            self.mscg_last_token_indices[:bs],
+                        )
+                        self.drafter.propose(
+                            target_token_ids=input_ids_buf[:num_tokens_eff],
+                            target_positions=positions_buf[:num_tokens_eff],
+                            target_hidden_states=self.mscg_outputs_A[:num_tokens_eff],
+                            num_reject_tokens=self.mscg_num_reject_A[:bs],  # Step 4: real num_reject (mscg_num_reject_A in capture)
+                            next_token_ids=next_tok,
+                            last_token_indices=self.mscg_last_token_indices[:bs],
+                            out_drafts=self.mscg_drafts_A[:bs],
+                        )
+                        # <<< MSCG P6 PROPER Step 3 <<<
+
+                    self.graphs_ms[(bs, max_q_len, K)] = graph_ms
+                    self.graph_logits_ms[(bs, max_q_len, K)] = logits_A
+                    torch.cuda.synchronize()
+        except Exception as e:
+            import logging
+            logging.getLogger("atom").warning(f"[MSCG P6] capture failed for K={K}: {e!r}")
+            self.mscg_capture_failed = True
+            self.graphs_ms.clear()
+        # <<< MSCG P6 simple <<<
+    # <<< ATOM_MSCG scaffold <<<
+
     @torch.inference_mode()
     def capture_cudagraph(self):
         start_time = time.time()
@@ -2039,4 +2297,10 @@ class ModelRunner:
                 f"Consider reducing gpu_memory_utilization."
             )
 
+        # >>> ATOM_MSCG hook >>>
+        _mscg_k = self._mscg_enabled()
+        if _mscg_k > 1:
+            self.capture_cudagraph_multistep(K=_mscg_k)
+        # <<< ATOM_MSCG hook <<<
+
         return time.time() - start_time, self.graph_bs
diff --git a/atom/model_ops/attention_mla.py b/atom/model_ops/attention_mla.py
index 89a74f9..baaffde 100644
--- a/atom/model_ops/attention_mla.py
+++ b/atom/model_ops/attention_mla.py
@@ -593,7 +593,7 @@ class MLAAttention(nn.Module):
             paged_kv_indices,
             attn_metadata.kv_last_page_lens,
             attn_metadata.max_seqlen_q,
-            num_kv_splits=16,
+            num_kv_splits=None,
             sm_scale=self.scale,
             work_meta_data=work_meta_data,
             work_indptr=work_indptr,
diff --git a/atom/model_ops/moe.py b/atom/model_ops/moe.py
index 2315d37..0ea45e1 100644
--- a/atom/model_ops/moe.py
+++ b/atom/model_ops/moe.py
@@ -432,7 +432,7 @@ class FusedMoEMethodBase(QuantizeMethodBase):
             self.fused_experts = FusedMoEModularKernel(
                 prepare_finalize,
                 # experts,
-                # layer.shared_experts,
+                layer.shared_experts if hasattr(layer, "shared_experts") and layer.shared_experts is not None else None,  # Q3.3 patch — enable shared-experts fusion (vLLM #24097 mechanism)
                 quant_config=self.moe_quant_config,
             )
 
diff --git a/atom/model_ops/rejection_sampler.py b/atom/model_ops/rejection_sampler.py
index 4697c0e..c609848 100644
--- a/atom/model_ops/rejection_sampler.py
+++ b/atom/model_ops/rejection_sampler.py
@@ -9,8 +9,8 @@ from torch import nn
 
 ATOM_ENABLE_RELAXED_MTP = envs.ATOM_ENABLE_RELAXED_MTP
 if ATOM_ENABLE_RELAXED_MTP:
-    RELAXED_TOP_N = 10
-    RELAXED_DELTA = 0.6
+    RELAXED_TOP_N = 9
+    RELAXED_DELTA = 0.5
 else:
     RELAXED_TOP_N = 1
     RELAXED_DELTA = 0.0
@@ -24,6 +24,9 @@ class RejectionSampler(nn.Module):
         target_logits: torch.Tensor,
         # [batch_size, 1]
         bonus_token_ids: torch.Tensor,
+        # MSCG step1: optional pre-allocated buffers for in-graph capture
+        out_token_ids: "Optional[torch.Tensor]" = None,
+        out_num_bonus: "Optional[torch.Tensor]" = None,
     ) -> torch.Tensor:
         # Ensure target_logits is contiguous. For greedy sampling, we can use
         # logits directly (argmax is the same for logits and probs), but we
@@ -38,6 +41,7 @@ class RejectionSampler(nn.Module):
                 f"{expected_num_tokens} (len(draft_token_ids)), but got {target_logits.shape[0]}"
             )
 
+        # MSCG step1 inplace
         output_token_ids = rejection_sample(
             metadata.draft_token_ids,
             # metadata.num_draft_tokens_np,
@@ -46,6 +50,8 @@ class RejectionSampler(nn.Module):
             None,
             target_logits,
             bonus_token_ids,
+            out_token_ids=out_token_ids,
+            out_num_bonus=out_num_bonus,
         )
         return output_token_ids
 
@@ -64,7 +70,11 @@ def rejection_sample(
     target_probs: torch.Tensor,
     # [batch_size, 1]
     bonus_token_ids: torch.Tensor,
+    # MSCG step1: pre-allocated output buffers (for in-graph capture)
+    out_token_ids: Optional[torch.Tensor] = None,
+    out_num_bonus: Optional[torch.Tensor] = None,
 ) -> tuple[torch.Tensor, torch.Tensor]:
+    # >>> MSCG step1 inplace >>>
     assert draft_token_ids.ndim == 1
     assert draft_probs is None or draft_probs.ndim == 2
     assert cu_num_draft_tokens.ndim == 1
@@ -80,13 +90,21 @@ def rejection_sample(
     assert bonus_token_ids.is_contiguous()
     assert target_probs.shape == (num_tokens, vocab_size)
 
-    # Create output buffer.
-    output_token_ids = torch.empty(
-        (batch_size, num_spec_steps + 1),
-        dtype=torch.int32,  # Consistent with SamplerOutput.sampled_token_ids.
-        device=device,
-    )
-    num_bonus_tokens = torch.empty(batch_size, dtype=torch.int32, device=device)
+    # MSCG step1: reuse pre-allocated buffers when provided (capture-safe)
+    if out_token_ids is not None:
+        output_token_ids = out_token_ids
+        # Buffer must be at least (batch_size, num_spec_steps+1) int32
+    else:
+        output_token_ids = torch.empty(
+            (batch_size, num_spec_steps + 1),
+            dtype=torch.int32,
+            device=device,
+        )
+    if out_num_bonus is not None:
+        num_bonus_tokens = out_num_bonus
+    else:
+        num_bonus_tokens = torch.empty(batch_size, dtype=torch.int32, device=device)
+    # <<< MSCG step1 inplace <<<
 
     if RELAXED_TOP_N <= 1:
         # Strict greedy path: draft must exactly match target argmax
diff --git a/atom/models/deepseek_v2.py b/atom/models/deepseek_v2.py
index a04dcfb..8205baf 100644
--- a/atom/models/deepseek_v2.py
+++ b/atom/models/deepseek_v2.py
@@ -921,7 +921,14 @@ class DeepseekV2MoE(nn.Module):
 
         with torch.cuda.stream(alt_stream):
             # final_hidden_states = self.routed_expert_forward(hidden_states)
-            shared_output = self.shared_experts(hidden_states)
+            # Q3.3 patch — skip shared_experts call when fusion enabled (it runs inside MoE kernel)
+            if (
+                self.n_shared_experts is not None
+                and not is_rocm_aiter_fusion_shared_expert_enabled()
+            ):
+                shared_output = self.shared_experts(hidden_states)
+            else:
+                shared_output = None
 
         final_hidden_states = self.routed_expert_forward(hidden_states)
         # shared_output = self.shared_experts(hidden_states)
diff --git a/atom/spec_decode/eagle.py b/atom/spec_decode/eagle.py
index 14056c0..59c954a 100644
--- a/atom/spec_decode/eagle.py
+++ b/atom/spec_decode/eagle.py
@@ -115,7 +115,10 @@ class EagleProposer:
         num_reject_tokens: torch.Tensor,
         next_token_ids: torch.Tensor,
         last_token_indices: torch.Tensor,
+        # MSCG P4: pre-allocated draft output buffer (capture-safe)
+        out_drafts=None,
     ) -> torch.Tensor:
+        # >>> MSCG P4 drafter >>>
 
         forward_context = get_forward_context()
         context = forward_context.context
@@ -130,9 +133,12 @@ class EagleProposer:
         positions = target_positions + 1
         hidden_states = target_hidden_states
 
-        draft_token_ids = torch.empty(
-            bs, self.mtp_k, dtype=next_token_ids.dtype, device=next_token_ids.device
-        )
+        if out_drafts is not None:
+            draft_token_ids = out_drafts
+        else:
+            draft_token_ids = torch.empty(
+                bs, self.mtp_k, dtype=next_token_ids.dtype, device=next_token_ids.device
+            )
         # return draft_token_ids.fill_(1) # for debug
         var = self.runner.forward_vars
         for i in range(self.mtp_k):
diff --git a/atom/utils/block_convert.py b/atom/utils/block_convert.py
index 7ebfa41..2d783e4 100644
--- a/atom/utils/block_convert.py
+++ b/atom/utils/block_convert.py
@@ -196,9 +196,12 @@ def kv_indices_generate_triton(
 
     ratio_pad = triton.next_power_of_2(ratio)
     blocks_per_tile = max(1, 4096 // ratio_pad)
-    max_num_blocks = (max_ctx_len + ratio - 1) // ratio
-
-    grid = (triton.cdiv(max_num_blocks, blocks_per_tile), bs)
+    # KEYSTONE: grid sized to FULL block_tables capacity (n_cols), not
+    # runtime max_ctx_len. This makes grid_dim_0 a CONSTANT for a given bs
+    # which is cudagraph-replay-safe even when max_ctx_len grows. The
+    # kernel"s data-driven store_mask (out_pos < ctx_len, where ctx_len is
+    # loaded FROM TENSOR) correctly skips writes for smaller actual contexts.
+    grid = (triton.cdiv(n_cols, blocks_per_tile), bs)
 
     kv_indices_generate_kernel[grid](
         block_tables,
diff --git a/atom/utils/envs.py b/atom/utils/envs.py
index 62bd1b0..d0e5e65 100644
--- a/atom/utils/envs.py
+++ b/atom/utils/envs.py
@@ -55,6 +55,11 @@ environment_variables: dict[str, Callable[[], Any]] = {
         "ATOM_ENABLE_ALLREDUCE_RMSNORM_FUSION", "1"
     )
     == "1",
+    "ATOM_USE_POST_ATTN_RMSNORM_MXFP4_FUSION": lambda: os.getenv(
+        "ATOM_USE_POST_ATTN_RMSNORM_MXFP4_FUSION", "0"
+    )
+    == "1",
+
     "ATOM_LLAMA_ENABLE_AITER_TRITON_FUSED_RMSNORM_QUANT": lambda: os.getenv(
         "ATOM_LLAMA_ENABLE_AITER_TRITON_FUSED_RMSNORM_QUANT", "1"
     )
